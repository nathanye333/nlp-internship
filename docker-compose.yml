services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: real_estate
    ports:
      - "3308:3306"
    volumes:
      - ./data/raw:/docker-entrypoint-initdb.d:ro
      - mysql_data:/var/lib/mysql

  api:
    build: .
    image: nlp-internship-api:latest
    ports:
      - "8000:8000"
    environment:
      API_LOG_LEVEL: INFO
    volumes:
      - ./data:/app/data:ro
    depends_on:
      - mysql
    restart: unless-stopped

volumes:
  mysql_data:
